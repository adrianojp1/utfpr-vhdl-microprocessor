// Setando limites e constantes
    ld %r7,33
    ld %r6,1

// Preenchendo a memória do 1 ao 32
    ld %r1,1
    ld [%r1],%r1
    add %r1,%r6
    cmp %r7,%r1
    jrult -4

// Atribuindo 0 aos múltiplos de r1 na memória começando com r1 = 2
    ld %r1,2
    ld %r2,%r1
    add %r2,%r6
    ld %r3,%r2
    sub %r3,%r1
    jrult -2